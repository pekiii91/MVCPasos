@model IEnumerable<MVCLicnaDokumentaPasos.Models.tblKorisnik>

@{
    ViewBag.Title = "Index";
}

<h2>Lista korisnika</h2>

<p>
    @Html.ActionLink("Kreiranje novog korisnika", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Email)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Lozinka)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tblPaso.IzdajePU)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Email)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Lozinka)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tblPaso.IzdajePU)
        </td>
        <td>
            @Html.ActionLink("Izmeni", "Edit", new { id=item.KorsinikID }) |
            @Html.ActionLink("Detalji", "Details", new { id=item.KorsinikID }) |
            @Html.ActionLink("Obrisi", "Delete", new { id=item.KorsinikID })
        </td>
    </tr>
}

</table>
<div>
    @Html.ActionLink("Naslovna strana", "Index", "Home3");
</div>
